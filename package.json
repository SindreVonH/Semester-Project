{
  "name": "action-house",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
  "dev": "vite",
  "build": "npm run build:scss && vite build",
  "preview": "vite preview",
  "build:scss": "sass --quiet-deps src/scss/styles.scss dist/styles.css --no-source-map"
},
  "devDependencies": {
    "eslint": "^9.16.0",
    "eslint-config-prettier": "^9.1.0",
    "eslint-plugin-prettier": "^5.2.1",
    "prettier": "^3.4.2",
    "vite": "^6.0.1"
  },
  "dependencies": {
    "bootstrap": "^5.3.3",
    "sass": "^1.83.0"
  }
}